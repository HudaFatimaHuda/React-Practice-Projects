// this is an optional file
// will wrap only about folder components 


function Layout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    );
  }
  

export default Layout
